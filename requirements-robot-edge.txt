# Robot Edge dependencies
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.0.0
httpx>=0.26.0
# Physical E-stop binding (M7): libgpiod v2 Python bindings. Robot-side
# container images only — install libgpiod2 from apt first (Ubuntu Noble).
# Workstation venvs must NOT install this (no libgpiod on Windows).
gpiod>=2.0
